import React from 'react';

// BackgroundAnimation is no longer used in the hero.
// The hero now uses a CSS dot-grid pattern directly in HeroStyles.
// This component is kept as a no-op for any remaining imports.

const BackgroundAnimation = () => null;

export default BackgroundAnimation;
